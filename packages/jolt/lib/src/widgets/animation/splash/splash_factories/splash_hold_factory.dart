import 'package:flutter/material.dart';

///
class SplashHoldFactory extends StatelessWidget {
  ///
  const SplashHoldFactory({
    required this.child,
    super.key,
  });

  ///
  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
