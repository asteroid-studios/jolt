import 'package:ui/ui.dart';

class ToggleGroup extends StatelessWidget {
  const ToggleGroup({super.key});
  const ToggleGroup.single({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
