import 'package:flutter/material.dart';
import 'package:flutter_jolt/flutter_jolt.dart';

class JoltWidgets extends StatelessWidget {
  const JoltWidgets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        JoltText(''),
        // JoltButton(),
        // JoltGrid(),
      ],
    );
  }
}
